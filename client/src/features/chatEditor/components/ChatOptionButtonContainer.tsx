import { ImageUploadButton } from "@/features/chatEditor/components";

const ChatOptionButtonContainer = () => {
  return (
    <div className="flex items-center gap-2">
      <ImageUploadButton />
    </div>
  );
};

export default ChatOptionButtonContainer;
